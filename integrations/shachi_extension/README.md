# Shachi-style task extension

The `src/shachi/env/japan_policy_scientist` and `src/shachi/agent` directories
contain the environment and agent boundary for the public-service simulation.
The small `src/shachi/__init__.py` file is a compatibility shim for static checks
and replay. Live agent runs should use the full framework API.
